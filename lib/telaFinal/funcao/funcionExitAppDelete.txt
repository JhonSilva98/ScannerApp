/*import 'dart:io';

import 'package:flutter/material.dart';

class AppLifecycleObserver extends WidgetsBindingObserver {
   List<String> pdfPathDelete = [];
   void addpdfPathDelete (String text){
    pdfPathDelete.add(text);
   }

  @override
  Future<void> didChangeAppLifecycleState(AppLifecycleState state) async {
    if (state == AppLifecycleState.inactive || state == AppLifecycleState.paused) {
      // Chame a função para deletar os arquivos
      await deleteFilesOnExit();
    }
  }

  Future<void> deleteFilesOnExit() async {
    try {
      for (var file in pdfPathDelete) {
        if (file is File) {
          await File(file).delete();
        }
      }
    } catch (e) {
      print('Erro ao deletar arquivos: $e');
    }
  }
}*/